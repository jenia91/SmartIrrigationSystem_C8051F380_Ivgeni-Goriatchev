## Dependencies (local, not committed)
- Vendor header: `vendor/initsysSPI.h`
- Vendor binary: `vendor/LcdSpi20.LIB`

These files are proprietary and must be obtained from the display/touch module vendor.
Place them under `vendor/` locally (the folder is git-ignored).
